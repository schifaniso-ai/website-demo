import React from 'react';

export default function CTA() {
  return (
    <section className="cta">
      <div className="wrap">
        <div className="eyebrow" style={{ color: 'var(--gold-bright)', marginBottom: 22 }}>
          Plan the Journey
        </div>
        <h2>
          Five countries, <em>one</em> unbroken wilderness.
        </h2>
        <div className="divider"></div>
        <p>
          Whichever direction you travel from the falls &mdash; north into the
          delta, south into the bushveld, or east across the plains &mdash; the
          wild keeps going.
        </p>
      </div>
    </section>
  );
}
